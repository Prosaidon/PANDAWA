using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AutoFire : MonoBehaviour
{
    /*[SerializeField]
    private Transform firePoint; // Lokasi tembakan

    [SerializeField]
    private GameObject bulletPrefab; // Prefab peluru

    [SerializeField]
    private float fireRate = 1.0f; // Kecepatan tembak per detik

    private void Start()
    {
        // Mengatur pemanggilan tembakan secara berkala dengan tingkat tembak yang diinginkan
        InvokeRepeating("Shoot", 0, 1 / fireRate);
    }

    private void Shoot()
    {
        // Instansiasi peluru pada lokasi tembakan
        GameObject bullet = Instantiate(bulletPrefab, firePoint.position, firePoint.rotation);
        // Tambahkan kode lain yang sesuai dengan logika tembakan
    }*/
}
